## p01_liquidity — instant unshield
- New on-chain program (devnet `6PfFkvj…`): `init_pool` / `deposit` / `withdraw` / `prefund` / `settle` / `update_params`
- `PrefundRecord` + `LpShare` state; settler keeper workflow (`run-settler.yml`) fires `settle()` every 10 min
- Mobile STARK unshield gained an `instant` flag — users no longer need to front ~0.85 SOL rent to unshield

## Per-wallet note isolation
- `StoredNote` carries `ownerPubkey`; notes are deterministic via `HKDF(walletSeed, poolPDA, counter)`
- Persisted `shieldCounters` map + `useActiveNotes()` hook scopes visible notes to the connected wallet
- `rescanPoolFromSeed()` helper for seed-based recovery after uninstall

## STARK migration complete (Groth16 removed)
- Transfer / split / emergency-unshield / cancel-subscription / merkle-update all routed through STARK (circuits 1–6)
- On-chain STARK verifier hits 1.4M CU cap with margin (P1 closed, DEEP-ALI shipped for C1–C5)
- BN254 → Goldilocks note format across mobile + extension (P8.1 / P8.2)
- Groth16 code paths removed from `zk_shielded` / `p01_zkspl` / extension / SDKs; `p01_trustless` deleted

## Stealth hardening (P4)
- Forced v2 hybrid stealth, v1 refused in send paths (specter-sdk)
- HKDF info bound to `(ephemeralPubKey, kemCiphertext)`
- WOTS+ wrapping for stealth spending keys
- SHA-512 long-term commitment primitive; SPHINCS+/SLH-DSA evaluated

## BLE P2P note sharing
- `FingerprintVerification` + `services/sharing` hardened (anti-MITM)
- `emergency_unshield_denominated_stark` removed (redundant with `unshield_denominated_stark`)

## privacy-sdk
- Goldilocks field + Poseidon (crypto/)
- `spendingKey` identity helper, `liquidity` module, shield/treasury updates, types

## Mugen mobile
- `CopyLink`, `LiveActivityList`, `PrivacyReceiptRN`, `TradeLifecycleCardRN`, shared styles

## Other
- Nym status badge
- `apps/weekly-update` Remotion pipeline for weekly update videos
- `@noble/*` v2 migration (P5.5) across the monorepo

## Stats
- 14 Solana programs · 6 Circom circuits · 6 STARK AIRs · 9 Arcis MPC circuits
- 3 client apps (mobile, extension, web)

## APK
`protocol-01-v0.9.9.apk` — standalone Android release build (96 MB / 100,860,376 bytes)
