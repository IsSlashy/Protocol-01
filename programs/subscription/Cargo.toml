[package]
name = "p01-subscription"
version = "0.1.0"
description = "P01 Subscription Program - Delegated recurring payments with on-chain validation"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "p01_subscription"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []
idl-build = ["anchor-lang/idl-build", "anchor-spl/idl-build"]

[dependencies]
anchor-lang = "0.32.1"
anchor-spl = "0.32.1"

[lints.rust.unexpected_cfgs]
level = "allow"
check-cfg = ['cfg(feature, values("anchor-debug", "custom-heap", "custom-panic"))']
