# Registre des fuites — Protocol 01

## Ce qu'est ce registre

Ce fichier est l'inventaire des **canaux** : tout chemin par lequel une valeur privée (le secret de note, le commitment, le lien dépôt↔retrait, l'identité du dépensier) peut atteindre un observateur. Rien ne se mesure « en général » : on ne mesure qu'un canal nommé.

**Règle unique : un canal absent de ce fichier est un défaut de ce fichier, pas un canal qui n'existe pas.** Un canal incertain reste inscrit, marqué `?`. Aucune affirmation ici sans `fichier:ligne` ou mesure nommée ; là où un nombre n'est pas mesuré, c'est écrit.

Le registre compte ~110 canaux ; ils sont groupés en **familles** pour tenir en un document lisible, chaque famille nommant ses membres. Les sections détaillées portent sur les familles ouvertes, pas sur chaque membre. Le passage web du 2026-09-15 au 2026-09-19 en a ajouté douze, trouvés en le faisant : B11, B12, C7, D14 à D19, E8 à E10.

Statuts : **O** ouvert · **?** inconnu · **F** fermé (et sous quelles conditions) · **F arbre** fermé dans l'arbre de travail et épinglé par un test vert, **pas déployé** : tant que `apps/web` n'est pas redéployé, la production garde le canal ouvert, et la fermeture ne compte qu'après une sonde post-déploiement (P12 `--since-slot` pour les racines, un navigateur réel pour les écrans).

**Une ligne qui porte un F nomme son test**, sous la forme `fichier` (`nom du test`) : le fichier est dans le dépôt (suivi par git, ou non ignoré), et le nom est un test que ce fichier déclare (un titre `it`/`describe` en TS, un `#[test] fn` en Rust, une étape de workflow). Un surnom, un fichier que git ignore, un nom qui n'est qu'un commentaire ou une ligne F sans test fait rougir `apps/web/__tests__/lib/ledgerRowsNameTests.test.ts`, qui tourne avec la suite web. Le jour où ce test est arrivé, il a trouvé 28 écarts dans 14 lignes F (journal rouge LEDGER-1, 2026-09-19) ; ils sont corrigés ci-dessous. Une seule fermeture change de sens : l'oracle temporel de D10, corrigé hors dépôt, n'est plus compté F.

## Le tableau

| id | ce qui fuit | statut | épinglé par |
|---|---|---|---|
| A1 | ~~C1/C3/C6 n'ont aucun masque~~ **FERMÉ 2026-08-31.** Les trois portent une région d'aveuglement ET une colonne de lift. L'attaque qui récupérait les 4 entrées privées de C1 échoue maintenant sur les trois, avec contrôle contrefactuel : *« la différence est le masque, pas l'arithmétique »* | **F** | `air_aware_recovery_c1.rs` (`the_mask_closes_the_columns_that_gave_up_all_four_witnesses`), `air_aware_recovery_c3.rs` (`c3_now_sits_with_c6_and_c7_on_the_under_determined_side`), `air_aware_recovery_c6.rs` (`the_mask_closes_the_four_columns_that_used_to_fall`) |
| A2 | ~~**C0 = secret de note.** récupération par Lagrange~~ **FERMÉ 2026-09-11.** Le C0 de 32 lignes est retiré du chemin d'expédition ; le C0 masqué (largeur 5, 512 lignes, 480 lignes libres, lift, randomizer) tourne sur le pipeline générique et `verify_stark_proof` exécute ses deux phases dans une instruction. Contrôle positif conservé : la forme legacy rend encore le secret exact `0x1DEAD0D0CAFE5678` (100 ouvertures contre 32 inconnues), la forme masquée est sous-déterminée (86 ouvertures + 1 égalité contre 512 inconnues). Redéployé sur devnet le 2026-09-12 (slot 497235406) : fermé dans l'arbre ET en production ; un C0 masqué du blob expédié accepté live (`5akV5gg4…`, slot 497239840, BENCHMARK-2026-09-13 §6) | **F** (arbre et devnet depuis le 2026-09-12) | `air_aware_recovery_c0.rs` (`the_legacy_c0_hands_over_the_secret`, `the_mask_closes_every_constrained_column_of_c0`), X1/X2/X5 : `stark/src/compact/zk_hiding.rs` (`the_free_claims_are_jointly_uniform_on_every_circuit`, `quotient_leaves_are_exactly_uniform_on_every_circuit`, `unopened_leaves_stay_uniform_given_every_opening_on_every_circuit`) |
| A3 | ~~**Jointure inter-circuits** : un secret récupéré via C0 rend le masque C7 sans objet~~ **FERMÉ 2026-09-11 avec A2.** Les deux voies (C1 le 2026-08-31, C0 le 2026-09-11) sont sous-déterminées ; il n'y a plus de circuit qui publie un secret réutilisable par un autre. Fermé sur devnet avec A2 le 2026-09-12 | **F** (arbre et devnet) | `air_aware_recovery_c0.rs` (`the_mask_closes_every_constrained_column_of_c0`), `air_aware_recovery_c1.rs` (`the_mask_closes_the_columns_that_gave_up_all_four_witnesses`) |
| A4 | ~~Système publié surdéterminé~~ **FERMÉ 2026-08-31.** Le fil entier est compté sur des octets réels, **par canal** — un total groupé cachait un déficit de 132 sur le canal A de C3. Les cinq circuits sont SOUS-déterminés sur le canal A ; C7 : 1 350 publiés contre 1 760 aléatoires | **F** (sauf canal B de C5, voir A5) | `stark/tests/full_wire_ledger.rs` (`the_split_that_decides_per_channel`, `the_whole_wire_counted_off_real_bytes`) |
| A5 | ~~Ouvertures quotient et FRI hors couverture du masque~~ **FERMÉ sur C1/C3/C6/C7 le 2026-08-31.** La colonne de lift rend **384 valeurs de quotient engagées sur 384** affines en un élément de masque, donc exactement uniformes ; la colonne randomizer couvre FRI et le terminal. ~~⛔ C5 reste OUVERT : ni lift ni randomizer~~ **FERMÉ sur C5 le 2026-09-11** : colonne de lift et colonne randomizer ajoutées (largeur 7 → 9), X2 (feuilles de quotient exactement uniformes, 8 circuits) et X5 (valeurs non ouvertes uniformes sachant tout le publié, 8 circuits) passent sur C5 ; le comptage canal B de la nouvelle forme est en cours de re-mesure (`full_wire_ledger`) | **F** sur les 8 | `stark/src/compact/zk_hiding.rs` (`quotient_leaves_are_exactly_uniform_on_every_circuit`), `full_wire_ledger.rs` |
| A6 | ~~Pas de sel FRI, feuilles Merkle non salées~~ **FERMÉ 2026-08-31, et le sel n'est PAS le remède.** Le coset de la LDE ne touche jamais le domaine de trace, donc chaque coefficient de Lagrange d'une ligne d'aveuglement est non nul à **toute** position engagée : une feuille non ouverte est exactement uniforme, min-entropie ≥ 2·largeur·63 bits, indevinable — donc le SHA-256 non salé la cache dans le ROM. 🚨 Sur un sous-groupe (`h = 1`) ce serait faux : `blowup` positions tomberaient sur des lignes de trace et `Z_T = 0` y annulerait tout le masque | **F** | `stark/src/compact/zk_hiding.rs` (`the_lde_domain_never_meets_the_trace_domain_on_any_circuit`, `every_committed_trace_value_is_exactly_uniform_in_one_blinding_element`) |
| A7 | **Colonne 9** = `hash2(secret, cycle)` : dérivé du secret, invariant d'une preuve à l'autre (`spend.rs:1152-1157`). ⚠️ **La borne « 96..383 » date de la profondeur 12 et n'a pas été re-dérivée** : à `CANONICAL_DEPTH = 11`, `FIRST_FREE_ROW` vaut 352, donc la plage témoin est 96..351 et non 96..383. À re-mesurer avant de citer, pas à corriger de tête | O | rien |
| A8 | **Le masque est fourni par l'appelant**, seule sa *longueur* est vérifiée (`compact.rs:9557-9562`) — **reste OUVERT**. ~~`draw_spend_mask` n'existe que sous `feature="wasm"`~~ **FERMÉ 2026-08-30** : renommé `draw_blinding_mask` (partagé C6/C7 depuis le 29-08) et sorti de `mod wasm_api`, gaté `feature = "csprng"` qui est **par défaut** (`lib.rs:112,:129`). C'était le vrai défaut : derrière `wasm`, chaque appelant non-wasm écrivait son propre xorshift déterministe, et un masque déterministe ne cache rien. Cinq appelants prennent le vrai | **O** (clause 1), **F** (clause 2) | rien pour la clause 1 ; clause 2 : `stark/src/compact/zk_hiding.rs` (`the_mask_every_other_measurement_assumes_is_uniform`) |
| A9 | **Canal subliminal** : 1 280 felts libres = 10 240 o choisis par le prouveur, publiés à jamais | O | rien |
| A10 | Nonce de grinding : ~`log2(travail)−22` bits choisis par preuve (`verify.rs:1386`, borne inférieure seule) | O | rien |
| A11 | **Preuves répétées** : ~+320 équations nettes par preuve sur un témoin fixe ; `prepareUnshieldJobV4` peut échouer après le prouvage | O | rien |
| B1 | **v3 publie le commitment en clair** (octet 80/160) ; v3 reste enregistré et l'extension *émet encore* des notes v3 | O | P1/P2 en CI (`v3-subscribe`) ; le repli web d'une note aveuglée vers cette paire : B12 |
| B2 | **Payee en clair** : octet 88 (v3) / 115 (v4) + clé de compte ; balayage vers le portefeuille mesuré (`C4MqLbEx…`, +8 s) | O | P10 (offsets ; branches PASS/FAIL v3 sans fixture) |
| B3 | Nullifieur en clair + **PDA `NullifierRecord` énumérables** : recensement complet des dépenses en un `getProgramAccounts` | O | `nullifier_canonical_bound` (soundness, pas confidentialité) |
| B4 | **`directions` (3 o) + 3 frères + `merkle_root` choisi** : bucket sur 8, borne d'ancienneté | O | **rien** — `spend_root.rs:239-251` n'assère que des littéraux ; il ne peut pas rougir ; mesure hors programme : P12/P13 sous `--max-root-age` (VERIFY-1), voir B11 |
| B5 | Aucun **délai de maturité** : `dynamic_delay` calculé puis jeté (`_v4.rs:565`) | O | rien côté programme ; côté émission, la porte de maturité du serveur : `issue-note.node.test.ts` (`refuses a leaf one slot short, issues it at the threshold, refuses an unknown slot`) |
| B6 | **Dépôt entièrement public** : déposant, commitment, index, racines (`LeafInserted`) | O | utilisé comme vérité de base par P4 ; le `msg!` d'index de `shield_denominated_v3.rs:499` : PROG-1 (décision fondateur, redéploiement) |
| B7 | État du pool : dénomination, `note_count`, **histogramme 32 époques**, anneau de 100 racines | O | P5 est un INFO ; **la sonde P5 n'existe pas** dans `p01-verify.mjs` |
| B8 | Abonnements : `subscriber_commitment`, **retailer**, rate/interval, longueur 196/228 (licence), 3 tailles de vault, événements `claim`/`pause` | O | offsets seulement |
| B9 | Emplacement `min_epoch` (octet 72) non lu par le programme : y écrire l'époque republierait le blinding 63 bits | O | constantes clients seulement |
| B10 | Notes **pré-blinding** : commitment reconstruit par énumération de quelques milliers d'époques ; ces notes ne peuvent se dépenser qu'en v3 | O | web : divulgation à l'écran avant toute dépense v3 d'une telle note (V3-1), `poolHandlersUnshieldV4.test.ts` (`falls back to the v3 prepare on the same request once the disclosure was shown`) ; extension, mobile : rien |
| B11 | **La racine nommée date la note.** Une dépense v4 nomme un `merkle_root` de l'anneau de racines du pool ; si le client prouve contre le chemin **sauvegardé** (pris juste après son propre dépôt, ou scellé dans la note par l'émission), la racine nommée désigne ce dépôt. Mesuré : sur 34 dépenses v4, une nommait une racine vieille de 4 insertions (`probe-stale-root-2026-09-15.log`) ; sur 30 dépenses v3, la racine nommée était 25 fois celle du dépôt de la note elle-même (`probes/logs/05-analyze.log`). Un trou dans l'historique lu par le client était exactement ce qui faisait choisir la racine sauvegardée. **Fermé sur le web par SPEND-1** (vérificateur G8 du 2026-09-19, `verify-SPEND-1-r1/verdict.log` : PASS) : un retrait ou un abonnement v4 prouve contre la racine de la carte de feuilles qu'il vient de lire, la courante ou, si le RPC est en retard, celle de l'anneau où cette carte s'arrête ; une racine sauvegardée, de n'importe quel âge, ne la remplace jamais et ne sert que si elle est la racine courante ; une carte qui s'arrête là où la racine sauvegardée de la note avait été prise n'est jamais prouvée ; une note qu'aucune carte lue ne place, même après l'unique relecture, est refusée (`HistoryIncompleteError` : rien n'est dépensé, réessayer) et n'atteint jamais la paire C1+C3 ; l'export ne scelle aucun chemin dont la racine est celle déposée avec la note. Résidus : la racine nommée date encore la lecture du client (l'écart de son RPC, au plus) ; le programme accepte toute racine de l'anneau, donc seul PROG-1 (décision fondateur, redéploiement) ferme pour tous les clients ; l'extension et le mobile ont leur propre règle ; une note pré-blinding passe encore par la route v3, qui préfère un chemin porté (elle y publie de toute façon son commitment, B10) ; la dépense d'âge 4 est permanente ; rien de cela ne compte en production avant le redéploiement de `apps/web` et une sonde P12 `--since-slot` | **F arbre** web (dépense : SPEND-1 ; émission sans racine : ISSUE-2 ; marche d'historique complétée : HIST-1) ; **O** extension, mobile, et pour tous les clients tant que le programme accepte toute racine de l'anneau (PROG-1) | émission : `apps/web/__tests__/api/issue-note.node.test.ts` (`carries no root and no path, so nothing in it dates the deposit`) ; marche : `apps/web/lib/privacy/pool/poolHistoryBackfill.test.ts` (`a truncated cold walk and a later delta past the budget are both completed`) ; mesure : `.github/workflows/ci.yml` (`Root age — a stale root must stay detected (pinned FAIL, age 4)`) ; dépense : `apps/web/lib/privacy/pool/spendRootIsCurrent.test.ts` (`saved older path, current root proved`, `a saved root of any age, on a note of either source, never replaces the root the leaf map folds to`, `a map that ends where the note's saved root was taken is never proved (m == k)`, `the circuit-7 SUBSCRIPTION prepare never proves a root tied to the note either`, `a note that no leaf map read places is refused with HistoryIncompleteError, never with a v3 needle`), `apps/web/lib/privacy/worker/poolHandlersUnshieldV4.test.ts` (`a note whose insert no walk read places is refused on circuit 7 and never reaches the C1 + C3 pair: the real walk and the real prepare`), `apps/web/lib/privacy/pool/poolExportNote.test.ts` (`ships no path whose root is the one filed with the note, when the walk ends where that root was taken`) |
| B12 | **Repli silencieux d'une note aveuglée vers la paire v3 C1+C3**, qui publie le commitment (B1) : quand le circuit 7 ne plaçait pas la racine (`PRE-FLIGHT FAIL`), le gestionnaire web reprouvait la même note sur C1+C3 sans le dire. 30 des 64 dépenses atterries sont v3 (`probes/logs/05-analyze.log`) ; combien sont passées par ce repli plutôt que par des notes pré-blinding n'est pas mesuré. Fermé sur le web par V3-1 : seule l'aiguille pré-blinding, sous le plafond du blinding legacy, mène à la paire, et seulement après une divulgation à l'écran (B10) ; une note aveuglée reçoit « rien n'a été prouvé ni envoyé, réessayez ». Résidus : les routes v3 explicites restent (aucun appelant web) ; une note aveuglée dont le pré-vol échoue ne se dépense plus tant que le RPC n'a pas rattrapé ; l'extension et le mobile ont leur propre routage ; les dépenses passées restent publiées | **F arbre** web ; **O** extension, mobile | `apps/web/lib/privacy/worker/poolHandlersUnshieldV4.test.ts` (`blinded note refused, not proved on v3`, `the refusal says nothing was sent and to retry, names no note value and carries no needle`), `apps/web/lib/privacy/worker/poolHandlersSubscribeV4.test.ts` (`refuses a root the rebuild could not place on a blinded note, and never reaches the pair`) |
| C1 | **`circuit_id` dans les graines du PDA de buffer** (chemin v1, celui de la production), dans les données de compte, et dans `msg!` v2 | O | `cu_budget.rs` — **exclu de la CI** (`:2085-2094`) |
| C2 | **CU consommés** distincts par circuit (phase 1 et 2) : écrits par le runtime, inatteignables depuis le programme | O | `cu_budget` (ratchet ; hors CI) |
| C3 | **Enveloppe** : 77 965 o / 78 chunks (C7) vs 147 038 / 148 (C1+C3), taille du dernier chunk, loyer du buffer, arité des entrées publiques | O | rien côté web (le padding 145 000 est mobile) |
| C4 | **Entrées publiques en clair chez le vérifieur**, deux fois, *avant* la dépense : nullifieur + `sha256(payee)`. Survit à un envoi abandonné | O | rien |
| C5 | **Payeur de frais** (`accountKeys[0]`) et jointure buffer↔dépense sur une clé | O | P6/P11 (P11 PASS uniquement sur `v4-relayed`) ; le repli web qui faisait du portefeuille le financeur : C7 |
| C6 | Empreintes montantes : récompense relayeur **constante 2 500 000**, delta d'escrow, discriminant relayé/direct, compteur de prix CU `i+1` **encore présent dans 2 paquets npm** | O | rien |
| C7 | **Repli de financement par le portefeuille.** Quand le financeur (flotteur ou relais) refusait ou ne répondait pas, `fundEphemeralForJob` finançait l'éphémère de dépense depuis le portefeuille, en silence (le refus n'existait que sous `neverExposeWallet`, câblé à faux) : la dépense et le portefeuille partageaient alors une arête publique. Fermé sur le web par FUND-1 : un travail de valeur 0 ne demande jamais le portefeuille ; une panne du financeur donne « réessayez, rien n'a bougé ». Résidus : l'extension garde son propre empilement (D12) et le mobile son propre financement. Fermés le 2026-09-19 (préparation du balayage web) : quatre textes par langue promettaient encore le portefeuille en repli (`pay.subscribe.funderBody`, `pay.subscribe.noFunderBody`, `pay.subscribe.costRentPart3`, `pay.page.limit1Body`) et disent maintenant que rien ne bouge ; un retrait relayé, qui ne finance rien, se déclarait `fundedBy: 'wallet'` et sa carte disait que le portefeuille avait payé : il se déclare `relayer`, le retrait direct demande toujours le financeur avec `neverExposeWallet`, et une réponse de financement qui nommerait le portefeuille arrête le retrait avant l'envoi. Action fondateur : vérifier que le build de production porte `NEXT_PUBLIC_P01_FUNDER_TICKET`, sans quoi chaque dépense refuse | **F arbre** web ; **O** extension, mobile | `apps/web/lib/privacy/pool/fundEphemeralForJob.test.ts` (`FUND-1: a value-0 job never asks the wallet when the funder refuses`, `FUND-1: a value-0 job never asks the wallet when the funder cannot be reached`, `FUND-1: a withdrawal whose funder refuses stops after prepare, and the wallet signs nothing`), `apps/web/lib/privacy/pool/unshieldV4ClientRouting.test.ts` (`a relayed withdrawal reports the relayer as its payer, and asks nobody to fund it`, `a direct withdrawal asks the funder with neverExposeWallet set, whatever the caller passed`, `a funding answer that names the wallet stops the withdrawal before execute`), `apps/web/__tests__/components/PoolPanel.test.tsx` (`a relayed withdrawal's card says the relayer paid, and never that the wallet did`), `apps/web/__tests__/pages/PayAppCopy.test.tsx` (`no string in either dictionary promises the wallet pays when the funder cannot`) |
| D1 | **Même IP, même clé RPC** pour le dépôt et le retrait ; rafale de ~150 tx en ~2 min | O | rien ; TRANSPORT-1 (décision fondateur) |
| D2 | Le **garde anti-liaison interroge le RPC** avec (portefeuille, financeur) au moment de la dépense (`shieldClient.ts:813,:854`). **2026-09-19**, web : la vérification du financeur du **dépôt** (`resolveFunderOfPayer`) est supprimée au profit d'un verdict local et pessimiste (RPC-1) ; celle du financeur de la **dépense** reste, mais ne part plus qu'au clic, en parallèle de la préparation, jamais au montage | **O** (financeur de la dépense ; extension, mobile), **F arbre** web (financeur du dépôt) | `apps/web/lib/privacy/pool/selfDepositedNote.test.ts` (`own deposit: reachable, with 0 RPC, even when the float funded the deposit`, `is already asked while the worker is still preparing`), `apps/web/lib/privacy/pool/noPointedNullifierRead.test.ts` (`no RPC parameter equals depositPayer`) |
| D3 | Résidu L4 : `getAccountInfo(nullifierPDA)` sur 4 chemins de préparation, y compris ceux qui échouent. **2026-09-19** : l'export, le retrait d'une note reçue et la préparation d'abonnement du web lisent l'ensemble du pool (RPC-1) ; le mobile aussi (MOB-RPC) ; Recover, qui nommait la clé de dépense de chaque note détenue : D18 | **F arbre** web et mobile ; **O** extension (EXT-RPC refusé) | web : `apps/web/lib/privacy/pool/noPointedNullifierRead.test.ts` (`export of a received note`, `withdrawal prepare of a received note`, `the web app source has none`) ; mobile : `apps/mobile/services/privacy/storeNullifierReads.test.ts` (`no body reads one account, derives a nullifier PDA, or logs one`) |
| D4 | **KV serveur** : portefeuille payeur → code → adresse de note ; **sans expiration, par décision**. Les jointures feuille ↔ paiement, code ou destinataire sont en D15 | O | jointures : D15 ; le reste (paiement → code jusqu'à la remise, `paid:<sig>` et `claim:<code>` gardés pour toujours, la réponse scellée sous `sha256(code)`) : VAULT-1, KVX-1 (décision fondateur) |
| D5 | **La graine du trésor dérive chaque note émise** : ensemble d'anonymat = 1 face à l'émetteur, et *dépensable* par lui | O | impossible en code aujourd'hui ; VAULT-1 puis MIG-1 (décision fondateur, clés) pour les notes futures ; les notes déjà émises restent recalculables, leur étiquette (TAG-0) aussi |
| D6 | **Le worker exporte la graine** (`handlePoolExportSeed`, `seedHex`), protégé par une chaîne littérale publique et `?treasury=1` | O | rien ; RESTOCK-1 (décision fondateur) |
| D7 | Disque : clé acheteur en clair, reçus de relais en clair, résidu v1 en clair, **longueurs des blobs scellés** non paddées. **2026-09-19** : le paiement en attente est scellé (D17) ; restent le repli v1 **indexé par le portefeuille** de `lib/pay/subscriptions.ts` et `lib/pay/handoffs.ts` sans session, les longueurs non paddées des autres magasins scellés (une tentative marquée par Recover porte le préfixe `attempt:`, 8 octets de plus qu'une marque de dépense), la clé acheteur en clair (`lib/pay/buyerKey.ts`), des enregistrements scellés non authentifiés ; extension : EXT-STORE (décision fondateur) | O | `storeEncryption.test` épingle la *persistance* du résidu |
| D8 | **Presse-papiers / feuille de partage** portent le porteur ; le web ne l'efface jamais | O | rien ; mobile : MOB-SEAL (décision fondateur) |
| D9 | Adresse de note **statique par identité**, sans diversification. **2026-09-19**, web : chaque achat fait émettre vers une adresse à usage unique dérivée du code (DEV-1) ; l'émetteur voit encore l'IP, le ticket et l'heure | **O** (hors émission web ; mobile), **F arbre** (adresse d'émission web) | `apps/web/lib/privacy/worker/issueAddress.test.ts` (`two issuances, unlinkable addresses`) ; mobile : MOB-SEAL (décision fondateur) |
| D10 | Le **relayeur voit tout** hors chaîne (payee, nullifieur, preuve, IP) — **reste OUVERT**, et c'est le gros morceau. ~~`/health` publie `busy`, les compteurs et la clé opérateur, sans auth~~ **FERMÉ 2026-08-31** : `/health` ne rend plus que `ok`, `operator` (déjà en chaîne, il signe) et `startedAt`. 🚨 Le pire n'était pas `busy` mais **`lastSpendAt`** — l'horodatage exact de la dernière dépense relayée, interrogeable par un GET non authentifié : en sondant chaque seconde on reconstruit la chronologie complète des dépenses passées par ce relayeur, puis on les apparie à la chaîne. Les statistiques sont derrière `P01_RELAY_STATS_TOKEN` et **échouent fermé** — variable absente, branche inatteignable. ⚠️ **2026-09-19** : ce correctif vit sous `/services/`, que git ignore (D10b) ; aucun test du dépôt ne l'épingle, il n'est donc plus compté fermé | O (la vue hors chaîne) ; l'oracle temporel est corrigé **hors dépôt**, non compté | rien dans le dépôt : `spend-relay.mjs` `/health` vit sous `/services/` (D10b) ; TRANSPORT-1 (décision fondateur) |
| D10b | 🚨 **`/services/` est ignoré par git** (`.gitignore:68`) : le nœud relayeur, sa validation de lot et le démon opérateur ne sont **pas sous contrôle de version** — aucun historique, aucune revue, aucune CI, et un correctif local ne part nulle part. C'est le composant dont dépend la jambe de dépense de toute revendication d'unlinkabilité. (Effet inverse au passage : les keypairs en clair de `frost-signer` ne sont donc pas commités) | O | rien ; TRANSPORT-1 (décision fondateur) |
| D11 | Graine de portefeuille : ni secret avant, ni secret après — une fuite unique déanonymise tout l'historique | O | rien |
| D12 | L'**extension** est une pile parallèle non auditée, et son agent IA POSTe le solde et le texte de l'utilisateur vers Groq et Google. 🚨 **Mesuré 2026-08-31** : elle porte un SECOND empilement de relais, vivant (`shared/store/shielded.ts:757-762` → `relayerWrapper.ts:162-203` → `relay.ts`), où **le portefeuille signe `wallet → éphémère` et s'en nomme payeur** (`relay.ts:484-495`, `:723-733`) — exactement l'arête que la pile web a supprimée en payant le relayeur depuis la note. L'en-tête du fichier affirmait le contraire quatre lignes au-dessus de sa propre réfutation ; corrigé, mais **l'arête est toujours construite** | O | rien — l'en-tête dit maintenant la vérité, c'est tout |
| D13 | **SNI/DNS** : contacter l'hôte du relayeur révèle l'opération avant toute preuve. Aucun proxy dans l'arbre | O | rien ; TRANSPORT-1 (décision fondateur) |
| D14 | **Identifiants de note à l'écran, dans les journaux, les réponses et les documents.** Le web affichait la feuille, le commitment et la date de dépôt de chaque note ; les chaînes i18n imprimaient côte à côte les feuilles dépensée, émise et financée ; `/api/issue-note` renvoyait `leafIndex` et `commitment` ; un benchmark public publiait la paire feuille financée → feuille émise d'un échange. Le code est public : un numéro de feuille sur une capture d'écran nomme le dépôt qui l'a créée. Fermé dans l'arbre : écrans et journaux web (UI-1, avec l'étiquette TAG-0 à la place), réponse d'émission (ISSUE-1), documents (DOCS-1), popup de l'extension (EXT-UI). Résidus : le fil principal garde `leafIndex` comme poignée ; les identifiants en chaîne (transactions, adresse de versement) restent à un clic ; le détail d'abonnement montre encore marchand et conditions ; la réponse 409 de `claim-for-payment` renvoie `leafIndex` et `foundAt` au client payeur ; des commentaires de source publics nomment encore des feuilles devnet précises, liées à un acheteur ou à une dépense (`app/api/issue-note/route.ts:1110`, `app/api/relay-to-buyer/route.ts:258`, `app/api/settle-till/route.ts:18`, `app/(pay)/app/page.tsx:55`) ; l'historique git garde l'ancien texte ; l'étiquette n'est pas un secret pour l'émetteur (D5). Mobile : MOB-UI refusé | **F arbre** web et extension ; **O** mobile | `apps/web/__tests__/lib/noteIdentifierTripwire.test.ts` (`renders, logs and reports no leaf, commitment or secret-derived value`), `apps/web/__tests__/components/PoolPanel.test.tsx` (`names the note received by its tag, and neither the spent nor the issued leaf`), `apps/web/__tests__/lib/i18n-parity.test.ts` (`no pay string prints a leaf number, in either locale`), `apps/web/__tests__/api/issue-note.node.test.ts` (`are not left expecting a field the reply no longer carries`), `apps/web/__tests__/lib/docsNoLeafJoin.test.ts` (`no funded-to-issued leaf pair in docs`), `apps/web/lib/privacy/pool/noteTag.test.ts` (`names no public identifier of the note`), `apps/extension/src/shared/noteIdentifierScan.test.ts` (`builds no string that carries a note value or a tree root, wherever it ends up`) |
| D15 | **Lignes KV au repos qui joignent une feuille à un acheteur.** Un dump du KV lisait `p01:note:issued:<pool>:<leaf>:to` (feuille → adresse destinataire, pour toujours), `contrib-claim:<pool>:<leaf>` (feuille → code), `claim-minted` = `contrib:<pool>:<leaf>:payment:<sig>` (feuille ↔ paiement), et la référence opérateur de `mint-claim` en clair. Mesuré en rejouant un cycle d'achat complet dans plusieurs mondes et en lisant ce qui fait bouger chaque ligne (RED-0). Fermé dans l'arbre : ISSUE-1 n'écrit plus `:to` et `scripts/scrubIssuedTo.mts` efface les anciennes lignes ; KV-1 retire les deux autres jointures et ne garde qu'un condensé de la référence ; les lignes qui nomment ensemble un code et un paiement sont effacées à la remise. **Ouvert** : clés `issued:<pool>:<leaf>` brutes et sans expiration, ensemble d'inventaire qui liste des feuilles, réservations indexées par la feuille avec `ex: 3600`, lignes `paid` et `relay` indexées par la signature brute ; au tournant de l'heure, l'expiration de la ligne de limitation de débit et celle des lignes `contrib-reserved` se répondent (jointure épinglée connue ; correctif `EXPIREAT` côté limiteur) ; deux sauvegardes comparées datent chaque émission et chaque remise (`p01:note:sealed:<sha256(code)>`, sans expiration) ; `p01:note:readiness:<pool>` (READY-1) garde le dernier créneau de 10 minutes où quelqu'un a demandé, et si une note était émissible ; `p01:pool-history:<pool>` (CACHE-1) ne porte que des données publiques de la chaîne, mais sa `newestSignature` date une requête. Fermeture : KVX-1 et VAULT-1 (décision fondateur) | **O** (clés brutes, dates, tournant de l'heure), **F arbre** (jointures feuille ↔ paiement, code, destinataire) | `apps/web/__tests__/lib/kvRowsAtRest.test.ts` (`no KV row joins a leaf to a payment, code or recipient after a full cycle`, `every finding is a known leak, and the list is now empty (a new join goes red)`, `at the hour turn, the findings are exactly the pinned expiry join (a new join, or its fix, goes red)`), `apps/web/__tests__/api/issue-note.node.test.ts` (`are deleted at redemption, and the payment gate is kept`), `apps/web/__tests__/api/mint-claim.test.ts` (`stores a digest for the default reference too, so the shape never varies`), `apps/web/__tests__/api/issue-note.test.ts` (`the sample row holds its bucket and a yes or no, nothing else, and never expires`) |
| D16 | **La clé d'instantané de l'historique contenait l'URL RPC, donc la clé Helius.** `poolHistoryKey` concaténait l'URL entière du point d'accès et le pool, et la ligne IndexedDB gardait cette clé : une URL Helius porte sa clé d'API, donc un dump de l'appareil la livrait. Fermé dans l'arbre : la clé ne garde que l'hôte, l'ancienne ligne est migrée puis effacée (HIST-1) ; la copie serveur partagée `p01:pool-history:<pool>` n'écrit ni point d'accès ni clé (CACHE-1). Résidus : une ligne d'appareil n'est migrée qu'au chargement du nouveau build ; deux fichiers `history-cache.json` de scratchpads locaux, hors dépôt, portent la clé en clair (action fondateur : les effacer) ; les marches de l'extension et du mobile ne sont pas auditées | **F arbre** web | `apps/web/lib/privacy/pool/poolHistoryCache.test.ts` (`no snapshot key or value holds the endpoint path or query`, `a legacy row keyed by the whole endpoint is migrated and deleted`), `apps/web/lib/privacy/pool/kvPoolHistory.test.ts` (`KV never holds the endpoint: no api-key, credential, path, user or host in any key or value`) |
| D17 | **Paiement en attente en clair sur l'appareil.** `p01:pending-contribution:v1` gardait portefeuille, feuille, code de réclamation, signature de paiement et preuve de réclamation en clair, sous un en-tête qui disait « aucun secret ». Fermé dans l'arbre par DEV-1 : un index en clair `{id, label, at, kind, paid, claimed}`, qui garde la purge qui avait corrigé un double paiement, et un corps scellé de longueur unique. Résidus : l'index montre l'étiquette d'identité, l'heure, le genre, l'état payé ou réclamé et le nombre d'entrées, donc le stade du cycle ; les enregistrements scellés ne sont pas authentifiés (D7) | **F arbre** web | `apps/web/lib/privacy/pendingContribution.test.ts` (`raw storage holds no wallet, payment signature, claim code, claim proof or leaf`, `the stored state does not move with the record values, and every sealed body has one length`) |
| D18 | **Recover nommait la clé de dépense de chaque note détenue.** Le clic Recover dérivait l'éphémère de dépense de chaque note, même jamais dépensée, et lisait ses tampons et son solde : le RPC apprenait d'avance le payeur d'une dépense future. Fermé dans l'arbre par RECOVER-1 : une tentative est marquée avant le financement (retrait, abonnement, échange), et le Recover par défaut ne lit que les notes marquées ou dépensées ; un bouton libellé « Vérifier chaque note (indique au RPC quelles clés sont les vôtres) » garde l'ancien comportement. Résidus : une marque de dépense venue d'un autre appareil lie ce navigateur à cette dépense ; la fenêtre de dépôt lit toujours les clés de dépôt, que ce navigateur envoie lui-même ; un échouage d'avant la mise à jour n'est atteint que par le bouton libellé | **F arbre** web | `apps/web/lib/privacy/pool/recoverReads.test.ts` (`default Recover names no untouched note’s spend key`, `moving the untouched notes to other leaves changes no request`, `withdrawal: a funding that failed after sending leaves a key the default Recover reads`) |
| D19 | **Compartiments de limitation de débit réversibles depuis un dump KV.** La clé `wl:rl:` hachait l'IP avec un sel public : avec le code public, une force brute sur l'espace IPv4 rend l'IP de chaque appelant de l'heure (mesuré, `bench-leak-at-rest.log`, 2026-09-15). Les routes lisaient aussi l'IP chacune à sa façon. Fermé dans l'arbre par RATE-1 **si** `P01_RATE_LIMIT_KEY` est posée : HMAC à clé sur la route, l'heure et l'IP, et un seul lecteur d'IP (`lib/net/clientIp.ts`). Sans la clé, la formule d'avant reste, et la réponse de disponibilité le signale sans couper le service. Résidu : un dump du KV **et** de l'environnement, clé comprise, teste encore des IP candidates contre les lignes non expirées (la dernière heure ; la dernière minute pour l'appairage). Action fondateur : poser la clé dans Vercel | **O** tant que la clé n'est pas posée en production (non vérifiable d'ici), **F arbre** avec la clé | `apps/web/__tests__/lib/rateLimitKey.test.ts` (`with P01_RATE_LIMIT_KEY set, the brute force with every public salt matches nothing in any route bucket`, `no file under app/api parses an IP header itself`) |
| E1 | Graphe monétaire : l'acheteur paie la caisse en clair ; **P11 ne remonte que 2 sauts** alors que la topologie en fait 4 | O | P11 (aveugle au-delà du flotteur) |
| E2 | Le règlement caisse→flotteur est une arête publique ; le plancher de lot est réglable à 1 par variable d'environnement. **2026-09-19** : une valeur 1 devient 2 (SETTLE-1) ; `P01_SETTLE_MIN_QUIET_SECONDS='1'` donne toujours une seconde de calme, et le plafonnement n'est pas visible de l'opérateur | **O** (arête caisse→flotteur ; calme sans plancher), **F arbre** (plancher de lot à 2 au moins) | tests d'unité purs ; plancher : `apps/web/lib/privacy/pool/settlementPolicy.test.ts` (`turns a configured floor of 1 into 2, and leaves 2, 5 and a malformed value alone`, `refuses to settle a till holding one purchase when the environment asked for a floor of 1`) |
| E3 | **Adjacence achat↔dépense** non décorrélée (le délai minimal ne couvre que `/issue-note`, pas `relay-to-buyer`) | O | rien |
| E4 | Réemploi d'éphémère : une souscription abandonnée et le retrait ultérieur partagent une clé. **Précisé 2026-08-31** — le réemploi est délibéré et argumenté (`recoverFloat.ts:83-87` ne balaie que `shield` et `unshield` ; un troisième domaine échouerait sa propre rente). L'argument « une note est dépensée une fois » tient pour la DÉPENSE et pas pour la TENTATIVE : un abonnement abandonné financé par le **portefeuille** (repli de `ephemeralFunder.ts:1249-1257`) suivi d'un retrait financé par le **flotteur** laisse un éphémère dont l'historique nomme les deux. ⛔ **Ordre imposé** : étendre le balayage de récupération AVANT de séparer, sinon on échange une liaison étroite contre du SOL perdu. **2026-09-19** : sur le web, le repli portefeuille qui ouvrait cette arête n'est plus atteignable (C7) ; l'extension garde le sien | O | rien — la condition est écrite dans `subscribeEphemeral.ts` ; le repli web : C7 |
| E5 | Le **stock du trésor est classifiable** (financeur, cadence, plages IP GitHub Actions) : `note_count` surestime la foule | O | rien ; journaux publics : E8 ; réassort : E10 ; RESTOCK-1, HOMOG-1 (décision fondateur) |
| E6 | L'**autorité d'upgrade** est la clé CLI par défaut, aussi trésorerie et nœud relayeur, et a payé le retrait vedette | O | `publicPayer.test` (refus contournable par variable d'environnement) |
| E7 | Un seul opérateur tient émission + relais de fonds + relais de dépense : carte complète hors chaîne | O | impossible en code ; VAULT-1, MIG-1 (décision fondateur) |
| E8 | **Journaux publics de GitHub Actions** (le dépôt est public, ses journaux aussi) : le réassort imprimait solde, jitter et `+1 leaf <index>` à chaque dépôt ; la recharge imprimait les clés publiques du flotteur et du portefeuille de réassort, leurs soldes et la signature ; `settle-till` recopiait tout le corps de la réponse ; les avertissements de bibliothèque y arrivaient aussi. L'heure publique de chaque passage apparie les dépôts du trésor aux passages. **Fermé dans les sources par CI-1** (vérificateur G8 du 2026-09-19, `verify-CI-1-r1-summary.log` : 17 mutants sur 17 comme voulu) : les deux workflows et leurs scripts n'impriment plus qu'un verdict fait de mots d'une liste fermée (`apps/web/lib/privacy/ciLog.ts`), un message de bibliothèque sort expurgé, `settle-till` ne recopie plus le corps, le réassort tourne sous `--silent` ; les cas « fausse chaîne » tiennent le secret du trésor comme le workflow le passe (`P01_TREASURY_KEYPAIR_JSON`, en JSON et en base58), et tout refus du script de recharge passe par `fail()`. Résidus : le test lit les sources, pas un passage réel (action fondateur : relire un passage où la recharge et le réassort ont tourné) ; l'heure des passages reste publique (HOMOG-1 et un horaire non public : décision fondateur) ; les journaux passés ne sont ni relus ni effacés (action fondateur) ; des nombres d'au plus deux chiffres atteignent le journal (le stock, déjà public par `GET /api/issue-note`) ; `ACTIONS_STEP_DEBUG` doit rester non posé ; un troisième workflow, et une bibliothèque qui bâtirait un message avec un secret reçu, sont hors du scan ; `GET /api/settle-till`, sans authentification, nomme le flotteur | **F arbre** (sources des deux workflows et de leurs scripts) ; **O** (heure des passages, journaux passés, aucun passage réel relu) | `apps/web/lib/privacy/pool/ciLogHygiene.test.ts` (`CI output names no key, signature, leaf, balance or jitter`, `the public workflows echo no response body and run the restock silently`, `the restock job and the top-up script print only through ciLog`, `the live restock, run against a fake chain, says nothing that moves with the unspent stock, the leaves, the balance or the random draws`, `the top-up script, run against a fake chain, puts nothing on the record that moves with the keys, the balances, the signature or the random draws`) |
| E9 | **Repli silencieux vers le dépôt propre.** Quand `/api/issue-note` ne répondait pas, le clic Shield déposait la note de l'utilisateur lui-même, sans le demander ; un dépôt propre publie son déposant (B6) et relie le portefeuille à la feuille. Fermé dans l'arbre par READY-1 : la lecture renvoie `issuableNow`, échantillonné une fois par créneau de 10 minutes et jamais sur le chemin de la requête ; si la réponse est non, un avis propose « Continuer » (le chemin du jour) ou le dépôt **libellé** « Déposer ma propre note (liée à mon portefeuille) » ; si elle est absente, seul ce dépôt libellé est offert ; aucun temps d'attente n'est affiché. Résidus : l'échange ne consulte pas `issuableNow` ; un déploiement sans émetteur coûte un clic de plus ; la ligne KV de l'échantillon est en D15 | **F arbre** web | `apps/web/__tests__/components/PoolPanel.test.tsx` (`issuableNow false with no choice: nothing is paid, and a notice offers Continue or a labelled own deposit, with no wait time`, `no answer from the deployment: no own deposit without a choice, and it is the only choice offered`), `apps/web/__tests__/api/issue-note.test.ts` (`no snapshot gives null, with no RPC on the request path; the sample runs in after()`) |
| E10 | **Le réassort ne peut pas démarrer son prouveur** (constaté le 2026-09-19, déjà vrai à HEAD) : `restockInventory.test.ts` tourne sous l'environnement node de vitest, où `Worker` n'existe pas, et n'installe pas le prouveur en processus que `liveRelayedShield.test.ts` installe ; le premier dépôt échoue donc sur `Worker is not defined` (sonde du vérificateur CI-1 r4 ; relu ici : aucune occurrence de `Worker` dans ce fichier, `new Worker(` dans `starkProver.ts`). Latent en production : sur les 50 derniers passages planifiés de `restock-inventory.yml` (du 2026-09-07 au 2026-09-19, API publique de GitHub lue le 2026-09-19), l'étape Restock n'a jamais tourné : 24 échecs à « Write the treasury keypair » (secret du trésor absent, stock sous le seuil ou inconnu), puis 26 succès où elle est sautée (secret absent, stock d'au moins 7). Le jour où le secret est posé et le stock passe sous le seuil, le réassort échouera sans rien réassortir ; l'erreur est expurgée, donc rien ne fuit, mais un inventaire qui s'épuise pousse les acheteurs vers le dépôt propre (E9) et rétrécit la foule (E5). Correctif : le même prouveur en processus que `liveRelayedShield.test.ts` | **O** | rien — aucun test n'exécute le prouveur du réassort |
| F1 | Poseidon : constantes **non conformes à leur provenance annoncée** (répétitions de différences, p≈2⁻⁵²) ; `MDS_MATRIX_T5` **n'est pas MDS** (60 mineurs singuliers) ; 30 tours pleins contre 4+22+4 documentés | O | vecteurs de sortie seulement |
| F2 | Capacité d'éponge 64 bits → collisions en 2³² sur chaque feuille | O | `zz_skeptic_leaf_width.rs` — `#[ignore]` |
| F3 | Plancher Fiat-Shamir : **C7 ≈ 47 conjecturé / 42 inconditionnel**, et **C7 est exclu du test** (`b1_deep_binding.rs`, `0..=6`, `[u32;7]`) | O | rien pour C7 |
| G1 | **Décroissance des mesures** : P11 dépend du plafond `--max-chunk-tx` et de la rétention RPC ; une absence expire en silence | O | rien |
| G2 | **Fermetures conditionnelles** : offset 115 ne vaut qu'à profondeur 15 ; bucket 0 ne vaut qu'en dessous de 4 097 feuilles ; la faille SPL v3 n'est latente que parce que les deux pools sont SOL | O | rien ne lit ces faits en chaîne |
| G3 | **La prose lue comme preuve** : commentaires périmés (lignes miroir « inutilisées » alors que `verify.rs:1937-1944` les consomme ; L4 décrit comme actuel), chiffre « 48-50 s » présent dans 5 fichiers sans artefact. **2026-09-19** : la règle est mécanisée pour ce registre (toute ligne F nomme un test qui existe) ; elle a trouvé 28 écarts dans 14 lignes F, corrigés | **O** (commentaires du code), **F arbre** (ce registre) | registre : `apps/web/__tests__/lib/ledgerRowsNameTests.test.ts` (`every row marked closed names a test that exists in the repository`) ; commentaires du code : rien de général |
| ?1 | C7 hors trace : décomposition du quotient, polynôme DEEP, engagement vectoriel — **aucun argument de simulation** (`spend.rs:262-268`) | ? | rien |
| ?2 | ~~C3 et C6 : jamais attaqués~~ **FERMÉ 2026-08-31 / 2026-09-11.** Attaqués par les mêmes harnais que C1 (contrôle positif puis masque) ; sous-déterminés sur canal A, uniformité X1/X2/X5 mesurée | **F** | `air_aware_recovery_c3.rs` (`c3_now_sits_with_c6_and_c7_on_the_under_determined_side`), `air_aware_recovery_c6.rs` (`the_mask_closes_the_four_columns_that_used_to_fall`), `stark/src/compact/zk_hiding.rs` (`unopened_leaves_stay_uniform_given_every_opening_on_every_circuit`) |
| ?3 | Système conjoint C1+C3(+C6) sur un témoin partagé : jamais formé | ? | rien |
| ?4 | Provenance du blob wasm : l'ascendance git prouve qu'il a été *commité* après le masquage, jamais qu'il en a été *construit* | ? | `wireFormat.test.ts` n'épingle que des longueurs, et le masquage est neutre en longueur |
| ?5 | Regrouper les tx par blockhash ; compteur Memo mobile ; CU de l'instruction de dépense ; 15 paquets npm publiés jamais audités ; sauvegardes OS mobiles | ? | rien |
| ?6 | Les deux programmes restent-ils upgradables aujourd'hui ? Non vérifiable depuis le dépôt | ? | rien |
| F-a | Aucun point de requête LDE n'est un point du domaine de trace (coset h=7) : la **copie verbatim** du commitment en colonne 9/5 est retirée | F | `stark/src/compact.rs` (`shift_is_outside_every_shipping_lde_domain`), `b7_coset_shift_pairing.rs` (`coset_shift_is_outside_every_shipping_lde_domain`) |
| F-b | L'instruction v4 ne porte **aucun commitment** | F | P1/P2/P4 sur `verify/fixtures/v4-live` + contrôle positif `verify/fixtures/v4-synthetic`, en CI : `.github/workflows/ci.yml` (`node verify/p01-verify.mjs --self-test --replay verify/fixtures/v4-live`, `Positive control — synthetic v4 spend must stay reported clean`) |
| F-c | `ood_z` et les positions de requête sont redérivés et comparés côté vérifieur | F | comparaisons explicites dans `programs/p01_stark_verifier/src/verify.rs` ; même règle de tirage du point hors domaine des deux côtés : `ood_resampling_parity.rs` (`both_sides_resample_the_ood_point_identically_on_every_geometry`) ; la preuve d'un témoin refusée sous les entrées publiques d'un autre : `c7_binding.rs` (`one_witnesss_proof_does_not_verify_another_witnesss_public_inputs`) ; positions de requête : aucun test isolé retrouvé le 2026-09-19 |
| F-d | L4 par note remplacé par **un seul `getProgramAccounts` par pool** — web ; mobile depuis MOB-RPC (compteur, rafraîchissement et pré-vols lisent l'ensemble du pool et échouent fermés) ; l'extension : EXT-RPC refusé (r3), non compté | **F** web ; **F arbre** mobile ; **O** extension | `poolResolveSpent.test.ts` (`asks the chain about the POOL, never about a note`) ; mobile : `apps/mobile/services/privacy/spentSet.test.ts` (`asks one question, filtered to this pool nullifier records, and names no note`, `fails closed: a rejected read throws, and hands back no counter`) |
| F-e | Pré-financement à montant exact : jitter côté web ; `jitterPrefund` n'existe pas dans `apps/mobile` | **F web / O mobile** | `prefundAmount.test.ts` (`erases the constant: 500 draws never reproduce the measured devnet amount`) |
| F-f | `shield`/`transfer`/`unshield` de base (C5) désenregistrés ; alias de nullifieur non canonique borné dans les 5 instructions vivantes ; ordre d'inventaire mélangé ; montant payé identique en relayé et direct | F | tests en CI : `unshield_c5_membership.rs` (`the_built_program_cannot_dispatch_unshield_at_all`, `the_built_program_cannot_dispatch_transfer_at_all`), `nullifier_canonical_bound.rs` (`every_stark_spend_bounds_the_nullifier_value_not_only_its_width`), `apps/web/__tests__/api/issue-note.test.ts` (`🚨 is not the configured order, so purchase order cannot be mapped to a leaf`) ; montant identique relayé et direct : aucun test nommé retrouvé le 2026-09-19 |

## Les familles ouvertes, en clair

**A1–A3 — les circuits sans masque.** Trois des quatre circuits vivants ne masquent rien. Sur C1 les 35 lignes de padding sont des copies d'autres lignes : 128 inconnues tombent à 93 effectives contre 110 ouvertures, et le système se résout par élimination gaussienne. Ce n'est pas un distingueur, c'est une **récupération** — et la valeur récupérée est l'autorité de dépense. **Mesure qui trancherait** : pointer le solveur existant sur des octets réassemblés d'un vrai envoi devnet et comparer le commitment reconstruit à la feuille publiée. **Coût de fermeture** : masquer C1/C3/C6 comme C7 (surface et CU en hausse), ou retirer les chemins v3 — ce qui échoue les notes pré-blinding.

**A4–A9 — le fil de C7 au-delà de la trace.** Le fil publie ~1 600 felts ; la marge « 128 contre 90 » n'en couvre que 900. Les 700 autres (quotient, FRI, polynôme final) sont des équations dans les mêmes inconnues, et aucun test ne les transforme en équations. **Mesure** : étendre `air_aware_recovery_c7.rs` au système conjoint, avec contrôle positif (masque effondré) sous peine de vert infalsifiable. **Coût** : sel FRI + feuilles randomisées = redéploiement du vérifieur et croissance du fil.

**B1–B4 — ce que l'instruction dit encore.** v3 publie le commitment ; v4 publie le payee, le nullifieur, 3 bits de bucket et 3 frères. **Mesure** : regrouper les dépenses enregistrées par `bucket_index(directions)` et publier la plus petite population. **Coût** : prouver 15 niveaux au lieu de 12 (CU), ou padder le chemin web.

**C1–C6 — l'enveloppe est plus bruyante que la preuve.** L'adresse du buffer nomme le circuit, les CU le renomment, l'arité le renomme encore, la taille et le nombre de chunks le confirment. `init_proof_buffer_v2` + `verify_uniform` existent et **la production ne les appelle pas**. **Mesure** : lire un journal de vérification vivant et les clés de compte d'une dépense enregistrée. **Coût** : migrer le client vers v2 + padding à 145 000 o — 145 chunks au lieu de 78, plus de loyer, et interaction avec le plafond de P11.

**D1–D13 / E1–E7 — hors chaîne.** Le fournisseur RPC voit les deux moitiés depuis une IP ; le KV joint le paiement à l'adresse de note ; l'émetteur peut recalculer *et dépenser* chaque note émise ; une seule clé publiée est autorité d'upgrade, trésorerie et relayeur. **Aucune mesure en chaîne ne tranche ces canaux** : ce sont des faits de garde et de topologie. **Coût** : séparer les rôles, faire tourner la clé, écrire une politique de rétention.

**F1–F3 — les paramètres.** Les constantes Poseidon ne correspondent pas à leur provenance annoncée (mesuré), `MDS_MATRIX_T5` n'est pas MDS (mesuré, aujourd'hui non câblé), et C7 — seul circuit de retrait vivant — n'a **aucun niveau de sécurité assuré** par un test.

**B11, B12, C7, D14–D19, E8–E10 — trouvés par le passage web du 2026-09-15 au 2026-09-19.** Le client choisissait la racine, le repli et le payeur ; l'écran nommait la note ; le serveur gardait des jointures ; la CI parlait. La plupart sont fermés **dans l'arbre** et épinglés ; aucun ne l'est en production tant que `apps/web` n'est pas redéployé. Restent ouverts : E10, la part non fermée de B11 (PROG-1, pour tous les clients), de D2, D15, D19 (tant que la clé n'est pas posée) et de E8 (heure des passages, journaux passés), l'extension et le mobile, et ce qui attend une clé ou une décision du fondateur : VAULT-1, MIG-1, KVX-1, RESTOCK-1, HOMOG-1, TRANSPORT-1, PROG-1, EXT-STORE, MOB-SEAL. **Actions fondateur** : redéployer `apps/web`, puis lancer P12 `--since-slot` sur une dépense postérieure ; poser `P01_RATE_LIMIT_KEY` ; lancer `scripts/scrubIssuedTo.mts` à blanc, puis avec `--apply` ; effacer les deux `history-cache.json` locaux et les journaux Actions passés ; vérifier `NEXT_PUBLIC_P01_FUNDER_TICKET` dans le build ; installer le prouveur en processus du réassort avant de poser le secret du trésor ; relire un passage réel de `restock-inventory.yml` où la recharge et le réassort ont tourné, et laisser `ACTIONS_STEP_DEBUG` non posé (E8).

## Ce que le masque de C7 NE couvre pas

Le masque écrit 1 280 felts CSPRNG dans les lignes 384..511 de la trace. Il ferme **un** canal sur plusieurs, et voici ce qu'il ne touche pas, d'après le code lui-même (`stark/src/air/spend.rs:262-268`) :

- la **décomposition du quotient**, le **polynôme de composition DEEP**, l'**engagement vectoriel** — aucun argument de simulation ;
- l'**absence de sel FRI** — chaque couche est engagée sans aléa dans la préimage ;
- les **lignes miroir** de la Route C, qui doublent R de 46 à 90 et consomment la moitié de la marge (et qui **sont consommées** par `verify.rs:1937-1944`, contrairement à leur commentaire) ;
- le **remplissage `hash2(secret, cycle)`** de la colonne 9, invariant d'une preuve à l'autre ;
- toute l'**enveloppe** (PDA, CU, chunks, journaux, payeur) ;
- **C1, C3, C6, C0**, qui n'ont pas de masque du tout.

Ce que la mesure du jour établit, et rien de plus : *la colonne « hold » de C7 ne se résout pas par le solveur linéaire de C1, calibré par l'effondrement du masque* (`air_aware_recovery_c7.rs`). Le fichier écrit lui-même qu'un résultat de rang n'est pas un résultat de secret, et que son masque de test est un xorshift déterministe. Cette phrase-là peut être gravée. « Rien ne fuit » ne le peut pas.

## Ce qu'on ne peut pas mesurer, et pourquoi

- **La garde des clés** : graine du trésor, graine de portefeuille, autorité d'upgrade, clé du relayeur. Aucun test ne peut établir qui les détient.
- **Ce que retiennent les tiers** : fournisseur RPC, relayeur, hébergeur, Groq/Google via l'extension. On ne peut mesurer que ce qui *part*, jamais ce qui est *gardé*.
- **La provenance du blob prouveur** : rien dans le dépôt ne reconstruit le wasm ; le masquage est neutre en longueur, donc aucun test de longueur ne peut le voir.
- **L'absence** : P11 à 0 sur 94 transactions et P10 « pas encore balayé » sont des absences ; une absence expire silencieusement et doit être *re-mesurée*, jamais citée.
- **L'état on-chain courant** (autorité d'upgrade, `next_leaf_index`, `token_mint`) : lu nulle part par un test.

## L'ordre de fermeture proposé

1. **Cesser d'émettre des notes v3** (extension, mobile) et compter les notes pré-blinding restantes. Aucun code cryptographique ; retire des notes de la classe A1/B1/B10.
2. **Rendre les épingles inertes réelles** : sortir `cu_budget` de la liste d'exclusion CI, faire lire à `spend_root.rs:239-251` le compte de feuilles réel, assérer C7 dans `b1_deep_binding.rs` (`0..=7`). Trois modifications mécaniques ; sans elles, C1–C2, B4 et F3 sont non gardés.
3. **Migrer le client vers `init_proof_buffer_v2` + `verify_uniform` + padding.** Déplace C1, C3, une partie de C2 et les journaux d'un coup ; coût connu : 145 chunks.
4. **Écrire les harnais manquants** : C3, C6, puis le système conjoint C1+C3, sur le modèle de `air_aware_recovery_c1.rs`, chacun avec son contrôle positif.
5. **Étendre le solveur C7 au quotient et à FRI** (A4/A5/?1). C'est la mesure la plus chère et la plus décisive : elle dit si le masque vaut quelque chose.
6. **Ajouter sel FRI et feuilles randomisées**, puis re-mesurer taille et CU. Redéploiement du vérifieur.
7. **Séparer les rôles opérateur** et faire tourner l'autorité d'upgrade (E6/E7). Décision organisationnelle, pas de code.
8. **Régénérer les paramètres Poseidon** depuis le script de référence, ou remplacer la phrase de provenance par ce qui s'est réellement passé (F1).
9. **Mécaniser la règle de preuve** : tout commentaire affirmant une propriété de confidentialité doit nommer un test ou un artefact, sinon la construction échoue (G3). C'est le défaut qui rend ce registre faux tout en le laissant vert.
