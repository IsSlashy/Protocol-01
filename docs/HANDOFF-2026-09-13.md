# Handoff — 2026-09-13

Branch `zk/uniform-masking-2026-09-11`, nothing committed today either. The
founder's uncommitted `apps/web` UI files are still in the tree and must not
be staged. Continues `HANDOFF-2026-09-12.md`; measured tables live in
`UNIFORM-MASKING-2026-09-11.md` (§4, §4a–c) and `BENCHMARK-2026-09-13.md`.

## 1. ZK — what is green and measured today

- The six re-pinned verifier suites RAN GREEN with the masks and the OOD
  resampling: b2_segment_binding 8/8, b4_pair_leaf 9/9, periodic_stride 7/7,
  route_c_trace_pair 22/22, liveness_generator_semantics 3/3 (1,275 s),
  honest_liveness 1/1 (160 witnesses × 9 arms incl. masked C0, 1,340 s);
  wire_parity 6/6, cross_circuit_confusion 22/22, b1_deep_binding 28/28.
- Channel B measured for all 8 (`full_wire_ledger` 5/5): C0m 247/512, C1
  328/512, C2 328/512, C3 268/512, C4 247/512, C5 290/1024, C6 268/512, C7
  247/512 published/randomizer, all OK; channel A C0m 720/1920, C2 990/1920,
  C4 810/1440, C5 1080/4608.
- CU re-measured with the resampling (cargo-build-sbf 3.1.9): every phase 1
  +2,630..+2,994 CU; pins re-pinned (`CuCeiling` rows, §4a). `cu_budget` full
  run in flight at the time of writing (`cu_budget_repin2.log`): the prose
  test and the caveat test were the two left red before the re-pin; fix the
  printed expectation if either still is.
- Docs updated: LEAK-LEDGER A2/A3/A5/?2 closed (tree, and devnet since the
  2026-09-12 redeploy), zk-simulation-argument §1/§2/§5 (identical view since
  the resampling; residual is the ROM step, bounded by X5).
- DONE 2026-09-12: verifier redeployed on the founder's go-ahead (slot 497235406), blob + TS pins shipped (§6).

## 2. Speed — measured, all on devnet, signatures in BENCHMARK-2026-09-13

- Transaction v1 (4,096 B, feature gate active on devnet since slot
  492,480,000, mainnet epoch 1035 ≈ 2026-09-15): 21 chunks instead of 80.
  STARK half: **7.8 s (C7) / 8.2 s (C6) on Helius**, single runs; 5 runs
  median 6.6 / 7.0 s, one 96 s outlier (a lost chunk waited the 90 s window).
  Public endpoint 11.4 / 19.5 s. Pre-L2 SDK path 35–41 s Helius, 73–85 s
  public. Legacy 1,232-byte fast path 14.5 / 16.1 s Helius, 63–66 s public.
- Full flows, the app's own worker handlers (`liveDevnet*.test.ts`, Helius):
  **shield 1 SOL 21.9 s** end to end; subscription v4 (one C7 proof) 437 s;
  unshield v4 358 s. The time is the **pool-history walk** (`fetchPoolCommitments`,
  `getSignaturesForAddress` + one `getTransaction` per signature, run twice
  per flow, ~90 s each), not the STARK.
- Fix landed: `apps/web/lib/privacy/pool/poolHistoryCache.ts` +
  `fetchPoolCommitments(.., { incremental })` — per (RPC, pool) cache,
  `until` the newest signature, IndexedDB in the worker, file store in the
  live shim (`P01_LIVE_HISTORY_CACHE`). 5 unit tests on real event bytes;
  91/91 pool tests; tsc clean. Cold-vs-warm measurement of
  `liveDevnetSubscribeV4` in flight (`live_flow_subscribeV4_{cold,warm}.log`).

## 3. Client changes today (all type-check; web 31/31 + 91/91)

- web `stark.ts`: `[L2-CLIENT]` keypair buffer in one tx, 8 concurrent
  chunks, merged phases for C3/C4/C6/C7; `[TX-V1]` v1 chunks via
  `txv1.ts` (kit compile + `WalletSigner.signBytes`), gate-detected, legacy
  fallback; six ephemeral signers got `signBytes`; `recoverFloat` scans the
  derived buffer address; `denominatedPool` v3 unshield uploads C1‖C3.
- extension `services/stark.ts` + `txv1.ts`, mobile `services/stark/index.ts`
  + `txv1.ts`: same two changes (mobile keeps its wave sender, 8/100 ms for v1).
- SDK `packages/stark-prover/src/upload-protocol.ts`: L2 buffer, merged
  phases, `ChunkPacing`, resilient sender, native v1 chunks (`txV1` option),
  `@solana/kit` 8.3.0 is now a runtime dependency; `scripts/live-timing.ts`
  (+ `v1-chunks.ts`) is the harness.
- `maxSupportedTransactionVersion` 0 → 1 in the pool-history readers of
  web/extension/mobile/sdk and the harness.

## 4. Legacy components found (founder's remark), evidence in BENCHMARK §5

1. Web subscribe/unshield fall back to the C1 + C3 pair (publishes the note
   commitment) when `prepareSubscribeJobV4` cannot rebuild; unshield takes v4
   only with `recipient` + `ownerPubkey`. The shield test's own subscribe
   step ran the pair (145.6 s). The dedicated v4 tests took the v4 route.
2. Note location = full history walk (fixed by the cache, to be measured).
3. Relayer batch (`buildStarkProofUploadBatch`) still PDA + resizes + 1,000 B.
4. `privacy-sdk` `instantUnshield.ts` carries its own copy of the old protocol — no app calls it; marked LEGACY in its header on 2026-09-13, pointing at `stark-prover`'s `uploadAndVerify`; not extended.
5. Circuit 0 on clients was the legacy 32-row pipeline until the redeploy — DONE 2026-09-12, the masked C0 is what every client now proves and what devnet accepts (BENCHMARK §6).
6. Mobile `submitAndVerifyStarkProofUniform`: no caller.
7. `scripts/unshield-note.mjs`, `test-shield-denominated.mjs`: circom/snarkjs.

## 4b. End-of-day numbers (BENCHMARK §5b)

After the history cache + `[SCAN-EPOCH]` + the 20 s / 1 s v1 confirm window,
from the app's own handlers on Helius, single runs: shield 24.4 s; subscription
v4 25.6 s (prepare + execute); unshield v4 22.7 s (prepare + execute). Then with
`[CLOSE-SWEEP]`: **shield 19.5 s, subscription v4 22.1 s, unshield v4 21.0 s**
(prepare 5.3 + execute 15.7; the whole unshield test, note reused, 25.5 s).
The unnamed live key keeps 0.5 SOL; the rest went back to the project key (~4.2 SOL). Whole
live tests: subscription 69.7 s (was 437), unshield 67.1 s (was 358); 16.6 s
of each is the harness reloading the service registry. `cu_budget` 22/22 with
the resampled CU rows. The unshield test's derived payee `7zVKXA4u…` now
holds ~3 SOL of devnet withdrawals (re-derivable from the unnamed key; sweep).

## 4c. Test status at the end of the day

- Rust: every verifier suite green incl. `cu_budget` 22/22; prover lib 191 + X5.
- `packages/stark-prover`: 67/67. Web pool suite (`vitest.pool.config.mts`):
  everything green EXCEPT 9 copy-disclosure tests (`sendFormDisclosureC7` ×7,
  `depositPublicity` ×1, `shieldCostDisclosure` "no longer promises the buyer
  a refund" ×1) that read `SendForm.tsx` / `PoolPanel.tsx` / `i18n` — the
  founder's uncommitted UI rewrite (thousands of lines) changed that copy; not
  touched, not mine to fix. `subscribeFloat` was red on a comment I had put on
  the RECORDED table's declaration line (regex parse) — fixed. `spendRouting`
  was red because its pin said mobile does not route v4 while mobile has since
  f8873d5d — flipped as the test instructs. Extension: 1 pre-existing failure
  (`ErrorBoundary` localStorage under Node 26), unrelated.

## 5. Next

1. DONE: cu_budget green, §5b written, v1 confirm window shortened.
2. DONE (web): `[CLOSE-SWEEP]` — `closeStarkProofBuffer(.., { sweepTo })`
   closes the buffer and sweeps balance + rent - fee in ONE transaction;
   `shieldV3`, `unshieldDenominatedStarkV4`, `subscribePrivateStarkV4` take
   `options.sweepTo` and the three ephemeral executors pass their sweep
   destination; the old two-step sweep stays as the fallback (it finds nothing
   to move). 3 unit tests, 89/89. Live re-measurement in
   `live_flow_*_closesweep.log`. Still open: fund the ephemeral concurrently
   with proving (~1 s); the 17 s harness gap before the unshield prepare.
   DONE: the history cache is ported to the extension and mobile
   `fetchPoolCommitments` (`poolHistoryCache.ts` twins, tsc clean); neither
   has a legacy epoch search, so `[SCAN-EPOCH]` is web-only by construction.
3. Legacy items 1, 3, 4 above. DONE: the harness payee (`7zVKXA4u…`, 1.99 SOL)
   and the unnamed live key (`FHtFQRTw…`, 1.996 SOL) were swept back to the
   project devnet key (`CeMo6i9V…`, `3tY6Z8yV…`); its balance is 5.70 SOL, the
   rest of the day's spend sits in devnet notes and vaults the tests opened.
4. Redeploy (go-ahead), blob, TS pins; then C0/C2/C4/C5 live.

## 6. Redeploy runbook — PREPARED 2026-09-12, EXECUTED the same day on the founder's go-ahead

EXECUTED 2026-09-12 after "tu a mon feu vert": deploy `3gt9vfoQ…` slot 497235406;
artifacts shipped (blob 0ad6d7f1, four twins, `target/deploy`); record re-measured
and the acceptance `2H5p3dqE…` (slot 497236376) written into
`deployed-verifier.json`; TS pins moved (`wireFormat.test.ts`, `prover-behaviour.mjs`,
`shippedBlob.test.ts`), stark-prover 67/67, `check:deployed:onchain` PASS, twins
PASS (the two gitignored specter-sdk copies refreshed by hand); web pool 842
passed with the same 9 founder-copy failures, extension 498/499 (pre-existing
`ErrorBoundary`), mobile 539/539. Live numbers in BENCHMARK §6. The runbook is
kept below as written before the deploy.


Everything below the "Steps" line touches devnet and waits for the founder's
explicit per-deploy go-ahead. Everything above it was done, read-only or
off-chain, so that the go-ahead is followed by commands and not by builds.

Staged artifacts, OUTSIDE the tree (`C:\Users\Slashy\.claude\jobs\77ca0ef2\tmp`,
deleted with the job — copy them into the tree at step 2, or rebuild with the
two commands, both deterministic on this machine):

| artifact | bytes | sha256 | how |
|---|---|---|---|
| `deploy-staging/p01_stark_verifier.so` | 801,472 | `e27317faccb28bd8…` | `cargo-build-sbf --manifest-path programs/p01_stark_verifier/Cargo.toml` (3.1.9, `build-verifier.log`); byte-identical to `target/cu-budget/p01_stark_verifier.so`, the .so `cu_budget` 22/22 measured |
| `wasm-staging/p01_stark_bg.wasm` + `p01_stark.js` | 265,324 + 20,189 | `0ad6d7f1eaed14a8…` | `wasm-pack build stark --target web --out-dir <staging> --release -- --features wasm` (`build-wasm.log`) |

Pre-ship parity, MEASURED (`wasm-parity-drive.log`, `wasm-parity-verify.log`):
the staged blob proved all eight circuits in Node through its own glue (C0 760 ms,
C1 738, C2 1,917, C3 1,224, C4 2,146, C5 8,716, C6 3,057, C7 1,716) and the new
`tests/wasm_blob_parity.rs` (ignored target, `P01_WASM_PROOFS_DIR=…`, excused in
`CI_UNRUN_TEST_TARGETS`, `cu_budget` 22/22 after the entry) verified every one
with THIS verifier, phase 1 and phase 2, 8/8. Wire lengths 74,365 / 94,897 /
95,777 / 79,597 / 75,085 / 91,261 / 82,477 / 79,405 — equal to the §4 table of
UNIFORM-MASKING byte for byte. C0 is 74,365 B (masked), not the 47,641 B the
deployed verifier accepts: the blob MUST NOT ship before the program.

Then the same eight proofs through the STAGED PROGRAM itself on litesvm
(`wasm_blob_parity::staged_wasm_proofs_verify_through_the_staged_program_on_litesvm`,
`wasm-parity-litesvm.log`): v3 buffer in one transaction, 3,840-byte chunks,
`verify_stark_proof_v2` + `verify_deep_ali_phase2` (C0: `verify_stark_proof`,
both phases inside, and the staged .so sets `deep_ali` for it too), close. All
eight accepted, both flags set. A local `solana-test-validator` was tried first
and refused: it needs a symlink this user session cannot create (Windows error
1314), so litesvm is the deepest off-chain leg available here.

| circuit | bytes | v1 chunks | phase 1 CU | phase 2 CU | transactions |
|---|---|---|---|---|---|
| C0 | 74,365 | 20 | 990,180 | in phase 1 | 23 |
| C1 | 94,897 | 25 | 1,039,766 | 390,811 | 29 |
| C2 | 95,777 | 25 | 1,045,457 | 312,593 | 29 |
| C3 | 79,597 | 21 | 880,067 | 169,060 | 25 |
| C4 | 75,085 | 20 | 856,932 | 365,094 | 24 |
| C5 | 91,261 | 24 | 982,141 | 438,551 | 28 |
| C6 | 82,477 | 22 | 902,441 | 175,970 | 26 |
| C7 | 79,405 | 21 | 890,407 | 193,476 | 25 |

Every CU figure sits under the `CuCeiling` rows of §4a (fresh masks, so a few
hundred CU of spread against `cu_budget`'s own samples). What this leg does NOT
give is wall clock on a network: the measured devnet v1 pipeline is 21 chunks
in 6.3 s (C7) / 22 in 4.4 s (C6) on Helius (BENCHMARK §2b); C2/C5 carry 25 / 24
chunks, so their upload leg is expected within a fifth of C7's. That is a
projection from chunk counts, not a measurement, and step 6 is where it gets
measured.

Chain facts, MEASURED 2026-09-12 on Helius devnet (`topup-authority.log`):
- programdata `Dmndh5d1…` has 840,213 B of space ≥ 801,472 → plain upgrade, no
  `extend`. Upgrade authority `7gWpzS…` = `~/.config/solana/id.json`.
- Buffer rent for the new ELF: 4.072 SOL, refunded when the buffer is consumed.
  The authority held 4.188 SOL; 0.45 SOL was moved back from the unnamed live
  key (tx `2hRvvFwu…`), so it holds 4.638 SOL and the unnamed key 0.05 SOL. The
  live tests after the deploy need the unnamed key refunded (≈1.5 SOL) — do it
  AFTER the upgrade returns the 4.07 SOL.
- ⚠️ `solana config get` points at MAINNET. Every command carries `-u devnet`.

Steps (founder's go-ahead first; `--program-id` on the command, never a fresh id):

```text
export PATH="$HOME/.local/share/solana/install/releases/3.1.9/solana-release/bin:$PATH"
solana program deploy -u devnet --program-id DGY37k3Jt7cbrfNa9rxyLZVcFB7S7A2NqtVpkh9fWQvs \
  --upgrade-authority ~/.config/solana/id.json --use-rpc --with-compute-unit-price 1000 \
  --max-sign-attempts 100 <staging>/p01_stark_verifier.so
# died mid-way?  solana program show -u devnet --buffers   → resume with --buffer <keypair it printed>
#                or recover the rent: solana program close -u devnet --buffers
```

1. Deploy (above). Record slot and signature in BENCHMARK §6.
2. Ship: `cp <staging>/p01_stark_verifier.so target/deploy/`; `cp <wasm-staging>/p01_stark_bg.wasm <wasm-staging>/p01_stark.js packages/stark-prover/wasm/`; `node packages/stark-prover/scripts/stark-wasm-twins.mjs --write`.
3. Record: `node packages/stark-prover/scripts/deployed-verifier-check.mjs --measure --cluster devnet`, paste the `deployed` block into `deployed-verifier.json`, generation stays `b2` on direct evidence (step 4), `accepts_client_blob_sha256` = `0ad6d7f1…` only after step 4.
4. Acceptance: `npx tsx packages/stark-prover/scripts/c7-live-proof.ts` (C7), then the same for a masked C0 proof — C0 is the circuit whose wire changed; write both transactions into `accepts_client_blob_evidence`.
5. Re-pin the TS twins: `wireFormat.test.ts` (C0 47,641 → 74,365; C0/C2/C4 digests retired like C1/C3's), `prover-behaviour.mjs` (`EXPECTED_DIGEST_ROWS` → `[]`, all eight masked, new lengths), `shippedBlob.test.ts` (`0ad6d7f1eaed`, 265,324), then `npm run check:deployed:onchain` in `packages/stark-prover`, its vitest, the web pool suite, the extension and mobile suites.
6. Live: `liveDevnetShield/SubscribeV4/UnshieldV4` (Helius, unnamed key) and `live-timing.ts --v1` on C0, C2, C4, C5 — the four circuits nobody has measured on chain with a mask.

## 7. After the redeploy — state at the end of 2026-09-12

- Devnet runs the uniform-mask verifier (slot 497235406); the shipped blob is
  0ad6d7f1; every gate that compares the two is green (§6). NOTHING COMMITTED:
  the branch carries ~80 changed files plus the founder's UI rewrite, which is
  untouched (its files were put back to the CRLF bytes they had after a
  line-ending pass touched them by mistake; every LF file the scripts had turned
  into CRLF is LF again).
- Measured on the new program (BENCHMARK §6, §6b, §6c): STARK half per circuit
  5.5–15.9 s (C5's 9.7 s is proving), shield 18.6 s, subscription v4 23.0 s,
  unshield v4 20.8 s. `live-timing.ts` drives all eight circuits now.
- Open, measured: the LEGACY C1 + C3 pair subscription spends 70+ s in
  `locateOwnedNote` with a warm history cache (BENCHMARK §6c). Legacy item 1.
- Open, not measured: the relayer server (`buildStarkProofUploadBatch`) still on
  the PDA + 1,000-byte path; the founder's 9 copy tests; the 16.6 s harness
  registry reload (not product).
- Devnet keys: unnamed `FHtFQRTw…` and the project key `7gWpzS…` — sweep the
  unnamed key back after the C1/C3 run; the payee `7zVKXA4u…` was swept.

## 8. The landing page as the product, and the generated visuals (2026-09-12, evening)

Founder's brief, after umbraprivacy.com and pay.loofta.xyz: land ON the
solution, no long copy, the docs explain themselves; then "text too big, the
app must be visible without scrolling on HD, Full HD, 2K, 4K"; then "generate
visuals with the Grok API in the site's identity".

- `apps/web/app/_home/HomeSimple.tsx` replaces HomeSections on `/`: overline,
  one serif headline (`pay.page.h1`), one line (`hero.desc3` + `hero.desc4`),
  the devnet `<PayApp>` inside `<WalletProvider network="devnet">`, the amber
  devnet line, three hairline links (docs, SDK, roadmap; `homeSimple.*` keys,
  en + fr). HomeSections.tsx stays in the tree, unrendered. MEASURED with
  Playwright on the dev server: at 1280x720 the stepper, asset picker and the
  whole connect card are inside the first screen; at 2560x1440 the links are
  too; 400px wide stacks cleanly. Header 4rem + hero ≈ 11rem at 720p; the hero
  scales with vh/vw (`.styx-hero-simple`).
- `__tests__/pages/Homepage.test.tsx` re-pinned to the new page (20/20; PayApp
  and WalletProvider stubbed). `lib/pay/flowProgress.ts` learned the
  "Rearming an existing proof buffer..." step the L2 path emits (its label
  sweep was red 3/150 on the pushed tree; 150/150 now).
- Visuals, xAI `grok-imagine-image-2.0` via `POST /v1/images/generations`
  (`XAI_API_KEY` from the environment, never printed), prompts built from the
  tokens in styx.css (ink #070709, paper #eae7df, accent #39c5bb, "no text, no
  logos, no people, no coins, no padlocks"). Kept, under `apps/web/public/styx/`
  (JPEG, 1280x720 / 720x1280 / 1024x1024): `river.jpg` behind the hero
  (replaces the animated RiverCanvas on `/` — a still costs nothing to run),
  `flank.jpg` on both sides of the app at ≥ 1600px (mirrored for the right),
  `share.jpg` as the ground of `app/opengraph-image.tsx` (rendered 1200x630,
  checked), `emblem.jpg` unused, kept as a candidate. The
  `grok-imagine-image-quality` model gave harsher, more literal results and was
  not used. Generator: one Python file, 30 lines, in the job scratchpad.

## 9. Notes that leave the list when they are spent or sent (2026-09-12, night)

Founder, after spending a note in a subscription: an older note (leaf 54,
deposited two weeks earlier, genuinely unspent) stayed listed and he read it as
the spent one; today's note (leaf 119) was spent at 20:32:44 and its nullifier
record is on chain in the shared `NullifierRecord` layout (41 bytes; v3 and v4
withdrawals and the v4 subscription all write the same PDA — checked). The rule
he set: "au moment où elle est dépensée ou envoyée, elle doit disparaître".

- `shieldClient.recordSpentNote` now dispatches `SPENT_NOTES_CHANGED_EVENT`;
  `PoolPanel` listens and merges `knownSpentNoteKeys` into `spentLocally`, so a
  subscription, an exchange or a withdrawal made in another tab drops the row
  at once, not at the next chain scan.
- A handed-over note leaves the list and the balance and sits behind a
  "Notes envoyées (N)" toggle (`pay.pool.showSent` / `hideSent`), with its
  un-mark button intact; the send form's `recordHandoff` already fires
  `HANDOFFS_CHANGED_EVENT`, so this is immediate too.
- Web suite 998/998. The exchange is measured in BENCHMARK §6d (28.1 s).

## 10. CI and the SDKs (2026-09-12, late)

CI on master was red on two jobs, both older than today's pushes:

- TS job: the nine copy-disclosure tests read the JSX for sentences that had
  moved into the dictionary when the app was translated. They now append the
  block they render (`en.pay.send` / `pay.page` / `pay.pool`) to the source
  they scan — 27/27 — and one of them caught a real defect on the way: the
  phone routes circuit 7 (f8873d5d) while the copy still said the phone
  publishes the commitment. `discPhone` (en + fr) now says what the phone does
  and names the one case where the commitment IS published (a note that
  cannot be proven on the newer circuit, from any client); the test's
  `STILL_V3` list is empty.
- Rust job (exit 101): `c7_pin_coverage::the_exclusion_list_does_not_outlive_its_reasons`
  — the guard's text detector saw `0u8..=7` in `b2_segment_binding` and asked
  for the exclusion entry to go. What it had seen was the k=8 pin written on
  2026-09-11 (`s4b_…`, a loop over `get_circuit_config`), a MENTION of C7: the
  seven segment-binding sweeps still stopped at C6 and `config_for` still
  panicked on 7, exactly as the entry said. Deleting the entry alone would
  have recorded a hole as closed. So the hole was closed: `phase2`,
  `generic_case`, `config_for` and `honest_variant` carry their `7 =>` arms
  (witness from `tests/common/mod.rs::w7`, forgery from
  `generate_spend_compact_proof_with_forgery`), every sweep runs `..=7`, and
  the entry and the count (5 → 4) went together. Measured (`b2_c7_run2.log`,
  8/8 in 48.4 s): C7 at w=12, k=8, n=512, 79,405 B; the segment-split
  forgery rejected with `FriFinalPolyDegreeTooHigh`, the aliased terminal
  with `FriTerminalCheckFailed` (21/22 queries on a non-zero terminal index);
  198 honest draws reach all 32 terminal indices. The only number that moved
  is the S5 row count, 28 → 32. Every other pin of the fast job passes
  locally one at a time; three of them "could not compile" only when run
  back to back with other cargo work on this machine.
- `restock-inventory` had failed six times a day since at least 2026-09-05 for
  two GitHub secrets that are unset (`P01_TREASURY_KEYPAIR_JSON`,
  `P01_FUNDER_SECRET_KEY`) while the pot held 318 notes; the job now reads the
  live stock first and ends green when there is nothing to restock. Adding
  the secrets is the founder's; the restock itself needs them.

SDKs, made current against the redeployed verifier (nothing published; `npm
whoami` is the founder's account and publishing is his call):

| package | local | npm | state |
|---|---|---|---|
| `@protocol-01/stark-prover` | 0.2.0 | 0.1.3 (pre-C7 blob, REJECTED by the current verifier) | README rewritten (keypair buffers, v1 chunks, merged phases, C7 row, measured timings), CHANGELOG.md created, dist built, `check:deployed:onchain` PASS, 67/67. `npm publish` from `packages/stark-prover` ships it. |
| `@protocol-01/specter-sdk` | 0.4.4 | 0.4.3 | inlines stark-prover's src at build; `wasm/` refreshed; README's two forbidden claims rewritten; 240/240; CHANGELOG.md created. ⚠️ the tsup DTS step segfaults on this machine (Node 26, same crash as Next's type-check); `dist/*.d.ts` are the 2026-09-06 files — rebuild on Node 24 or CI before publishing. |
| `@protocol-01/privacy-sdk` | 1.0.6 | 1.0.5 | `maxSupportedTransactionVersion: 1`; `instantUnshield` marked LEGACY; 112/112; build clean; CHANGELOG.md created. Its proofs come from the host's `generateStarkProof`, so it is current when the host runs stark-prover 0.2.0. |
| `@protocol-01/react-native-zk` | 2.0.0 | not published | wasm twin refreshed by `stark-wasm-twins.mjs`. |
| p01-js, zk-sdk, zkspl-sdk | unchanged | = npm | re-export stark-prover / carry the verifier id (unchanged `DGY37k3J…`). |

## 11. Four programs closed on devnet (2026-09-13, founder: "il faut les détruire")

Measured against the live clients first (`grep` of every program id through
`apps/`, `packages/`, `services/`, `programs/`, tests excluded): the web app,
its API routes and the pool program itself touch only `zk_shielded`,
`p01_stark_verifier` (the pool's sole CPI target) and `p01_registry` (read by
the subscription panels, 11 references); `p01_relayer` serves the extension
and mobile relay paths and the refund processor of `services/relayer`. Nothing
that ships sends an instruction to the other four. Closed with
`solana program close … -u devnet` from the authority `7gWpzS…`
(`close_programs.log`, session scratchpad); the ids are unusable for good:

| program | id | programdata | reclaimed |
|---|---|---|---|
| specter | `FgKhXakZGsd4PdiGgACYy8gwj1JLMYA691yQr2PhUNfL` | 792,168 B | 5.51469336 SOL |
| p01_quantum_vault | `9yVr79XkwGabckVxedz4UH78twzkgmGqXHBAX7vfJvYv` | 503,336 B | 3.50442264 SOL |
| p01_quantum_wallet | `D7RBAFcMq2gGddwFvabZKuJB1eWZFvESVrcJ3i15FFtz` | 328,376 B | 2.28670104 SOL |
| p01_fee_splitter | `UdxXEvcAzmGsqUtoBgnNkbmfnky4En2kLxNnsVQU5BM` | 369,480 B | 1.87783724 SOL |

Authority balance 1.6058 → 14.7895 SOL. What now points at a closed program
and must be removed or repointed in the repo: `Anchor.toml` (four devnet ids,
plus the never-deployed mainnet ids of specter and fee_splitter),
`packages/specter-sdk` (`PROGRAM_IDS`, the on-chain stealth client in
`client.ts`, `stealth/scan.ts` → `fetchV2Announcements`, the two indexers),
`packages/privacy-sdk` (`modules/vault.ts`, `feeSplitter`/`quantumVault` in
`constants.ts` and `types.ts`), `apps/mobile` (`services/quantumWallet`,
`stores/quantumWalletStore.ts`, `app/(main)/(wallet)/quantum.tsx`),
`apps/extension` (`store/shielded.ts` line 958, the stealth scan through
`scanForPayments`), the four crates under `programs/`, and the marketing data
of `app/parcours` and `app/styx-c`. Removing them is an API break for
privacy-sdk (vault module) and specter-sdk (stealth on-chain client): major
bumps, not patches.

Mainnet rent measured 2026-09-12 22:15 UTC on `api.mainnet-beta.solana.com`
(650,240 lamports + 5,080 per byte; devnet returns the same): what remains is
`zk_shielded` 6.8796 + `p01_stark_verifier` 4.2689 + `p01_registry` 2.0179 =
13.1664 SOL for the web flow, 16.2056 SOL with `p01_relayer`.

## 12. The repo after the closure (2026-09-13, "corrige l'API pour qu'elle soit clean")

Everything that pointed at the four closed programs is gone; measured after
each cut, logs in the session scratchpad.

- Rust: `programs/specter`, `programs/p01-fee-splitter`, `programs/p01_quantum_vault`,
  `programs/p01_quantum_wallet` deleted with their workspace entries, the
  tracked `target/idl` + `target/types` files of three of them, and the root
  mocha tests whose sole subject they were (`specter`, `fee-splitter`,
  `p01-quantum-vault`, `e2e-quantum-vault`, `e2e-stealth-payments`).
  `Anchor.toml` lost their ids in all three cluster sections. CI's
  `cargo check --workspace --all-targets --exclude p01-arcium --exclude encrypted-ixs`
  finishes in 13.3 s (`cargo_check_purge.log`); `Cargo.lock` follows. Six
  mixed root e2e/stress tests still name the four among live programs
  (`e2e-cross-program`, `e2e-privacy-modes`, `e2e-stream-secure`,
  `stress-crypto-sdk`, `stress-programs`, `zk-e2e`); none runs in CI.
- `@protocol-01/privacy-sdk` 2.0.0: `modules/stealth.ts` and `modules/vault.ts`
  deleted (every instruction went to specter / the quantum vault), with
  `PrivacySDK.stealth`/`.vault`, hooks, types, events, error codes 3xxx/7xxx,
  seeds and the `specter`/`feeSplitter`/`quantumVault`/`arcium` program ids;
  `PayrollModule` now refuses a stealth-addressed employee before any payment
  goes out. 112/112, `tsc` clean, build with DTS clean
  (`privacy_purge_after_tests.log`, `privacy_purge_build2.log`).
- `@protocol-01/specter-sdk` 0.5.0: `stealth/scan.ts`, `stealth/announcement-v2.ts`,
  `indexing/stealth-indexer.ts`, `transfer/claim.ts` and the whole `streams/`
  module deleted (the streams instructions lived in the specter program, not
  in `p01_stream`); `P01Client` keeps wallet, balance, `generateStealthAddress`,
  `sendPublic`, cluster/registry/relayer; the off-chain stealth math, the
  registry and service-registry clients, relay, subscription, proving and the
  commitment indexer stay. 189/189 (51 tests covered removed code), `tsc`
  clean (`specter_purge_tests_final.log`). `dist/` NOT rebuilt here (tsup DTS
  segfaults on Node 26): CI/Node 24 rebuilds it before any publish.
- `apps/extension`: `scanStealthPayments` returns an empty result with a dated
  comment; 498/499 with the same pre-existing `ErrorBoundary` failure as
  before, `tsc` clean, eslint clean (`extension_purge_tests_final.log`,
  `extension_lint_purge.log`).
- `apps/mobile`: the quantum screen, its boot provider, store and service
  deleted; 539/539, `tsc` with the same 35 pre-existing errors as before
  (`mobile_tests_after.log`, `mobile_tsc_after.log`). Noticed: mobile's
  `subscriptionContract.ts` targets `5kDjD9…`, Anchor.toml's LOCALNET id for
  `p01_subscription`, and an executable does exist there on devnet while the
  declared devnet id `3eDvPJ…` is absent.
- `apps/web`: parcours and styx-c data lost their specter / quantum-vault
  entries and the parcours step 02 now describes the recipient hash and the
  registry; the docs page lost its quantum-wallet topic and its SDK sample
  names `fetchAllServices` instead of `sendPrivate`; the sdk-demo stealth
  sample uses `generateStealthAddress(meta)` + `deriveStealthPrivateKey` and
  no scan; the roadmap lost the quantum-wallet item; both dictionaries
  follow. Default suite 996/996, rendering suite 480/480
  (`web_default_purge.log`, `web_ui_purge.log`).
- Root `scripts/` is git-ignored (`.gitignore:67`): the edits to
  `sync-program-ids.ts` and `build-idl.mjs` live on this machine only.
- Two founder-reported web bugs fixed on the way (commits c66bd511, 1feb8b43,
  ee1556fa): the background wrapper's class name collided with the form-field
  class (`styx-field` → `styx-ground`), and the cursor tension never released
  because `pointerleave` does not fire on `window`; the wake is now four
  followers and the eases are frame-time scaled.

Still declared but not deployed anywhere (out of this pass, founder's call):
`p01_stream`, `p01_subscription`, `p01_whitelist`, `p01_zkspl`, `p01_liquidity`
and the SDK modules that target them (privacy-sdk `streams`, `subscriptions`,
`confidential`, `liquidity`; zkspl-sdk).
